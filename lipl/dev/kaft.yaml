apiVersion: k8s.bitwarden.com/v1
kind: BitwardenSecret
metadata:
  name: kaft-stg
spec:
  organizationId: "a2b57f3d-6e2b-4467-b499-b31e00bfd804"
  secretName: kaft-stg
  authToken:
    secretName: bitwarden-sec
    secretKey: token
  map:
    - bwSecretId: "6238dfba-d286-4cd5-88e9-b4b20085c1b7"
      secretKeyName: internal-jwt-secret
# lipl-devはPRごとの一時環境で対応するkaft PR環境が存在しないため、
# 共有・安定環境であるkaft-stgに接続する（KAFT_BASE_URL等はlipl-api.yaml参照）。
