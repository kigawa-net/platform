apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - lipl-api.yaml
  - lipl-frontend.yaml
  - mariadb.yaml
  - kaft.yaml
  - lipl-backend-keycloak.yaml
  - lipl-session-encryption-key.yaml

# namespace・imageタグはPRごとにArgoCD ApplicationSetが動的に上書きする
# （apps/lipl-dev-appset.yml参照）。ベース内には固定値を持たせない。
